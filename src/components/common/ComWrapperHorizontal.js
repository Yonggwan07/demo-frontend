import { Box } from '@mui/material';

const ComWrapperHorizontal = ({ children }) => {
  return (
    <Box className='wrapperHorizontal'>
      {children}
    </Box>
  );
};

export default ComWrapperHorizontal;
